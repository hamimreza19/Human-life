#include<stdio.h>

int main()
{
 float width,length,area;
 printf("Enter the width and length : ");
 scanf("%d",&width,&length);

area= width*length;
printf("Area of rectangle : %d\n",area);

return 0;

}
